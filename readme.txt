 In order for to run these test you need to create a virtual environment either use pycharm or any other IDE, then install pytest via requirements.txt via command in terminal


 pip install -r requirements.txt

 then you can run all the test via

 pytest -v -s
